openapi: 3.0.0
info:
  title: MyProject API
  version: 1.0.0
servers:
  - url: http://localhost:5000/api
paths:
  /news:
    get:
      summary: Получить новости
      responses:
        '200':
          description: Успешный ответ
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /achievements:
    get:
      summary: Получить достижения
      responses:
        '200':
          description: Успешный ответ
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /support:
    get:
      summary: Получить поддержку
      responses:
        '200':
          description: Успешный ответ
          content:
            application/json:
              schema:
                type: object
                properties:
                  message:
                    type: string
  /table-data:
    get:
      operationId: getTableData
      summary: Получить таблицу
      responses:
        '200':
          description: Текущее состояние таблицы
          content:
            application/json:
              schema:
                type: array
                items:
                  type: array
                  items:
                    type: string
  /table-data/cell:
    post:
      operationId: updateTableCell
      summary: Обновить одну ячейку
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required:
                - row
                - col
                - value
              properties:
                row:
                  type: integer
                col:
                  type: integer
                value:
                  type: string
      responses:
        '200':
          description: Ячейка сохранена
          content:
            application/json:
              schema:
                type: object
